.env
__pycache__/
*.pyc
*.log

# Client's proprietary source code (NDA) -- must never be committed or
# pushed, especially since this repo is intended for a public GitHub remote.
betgenius/

# Handoff pack contents (SHARPAI_USAMA_PORTA_PACK.zip, received 2026-09-14) --
# real production TypeScript (mlb_phase1_policy.ts, mlb_cache_gate.ts, etc.)
# and internal wiring docs, same NDA/proprietary status as betgenius/ above.
code/
00_START_HERE.md
01_STATUS_AND_NEXT.md

# Pooled-model research (Model A/B, XGBoost pooling, accuracy-vs-ROI check) --
# out of scope per current client direction ("no new research/models for now").
# Kept on disk for reference, untracked from git so it doesn't ship in the
# next clean push.
reports/ml_model_walkthrough.html
reports/xgboost_pooled_A_output.txt
reports/xgboost_pooled_B_output.txt
reports/favorite_accuracy_vs_roi_output.txt
scripts/xgboost_pooled_A_factors.py
scripts/xgboost_pooled_B_multiyear.py
scripts/check_favorite_accuracy_vs_roi.py

# Raw client data exports (112MB xlsx x2 copies, real production odds data) --
# never commit these directly; see note in the report about the duckdb copy.
harness_out/
Desktop/
MANIFEST.txt

# The db now contains the full 2.42M-row client odds warehouse (loaded
# 2026-09-12) as well as the original smaller pick_history snapshot -- kept
# local-only per an explicit decision not to publish that volume of real
# client production data. Regenerate locally with scripts/load_client_odds_warehouse.py
# (needs the client's xlsx export) for the full version, or scripts/pull_pick_history.py
# + compact_db.py for the original smaller public-repo-era version.
db/mlb_markets.duckdb

# Client's internal handoff docs -- written for a hired contractor under NDA,
# not for public distribution.
Phase1-MLB-Developer-Handoff.md
START_HERE.md
